=begin
show primes beneath
=end

= is_prime (function nombre
    {
      (= aux (function x
        {
          (cond
            [{(> (* x x) nombre)} {true}]
            [{(== (% nombre x) 0)} {false}]
            [true {(aux (+ x 1))}]
          )
        })
      )
      (aux 2)
    }
  )
Array.iteri (Array.make 98 unit) (function item index {(= plus (+ index 2)) (if (is_prime plus) {(println plus)})}))
