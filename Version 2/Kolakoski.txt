:= valeur 1
:= liste [2 2]
:= pointeur 1
:= size [2]
:= update {
	(times (at_arr liste pointeur) (append liste valeur))
	(:= valeur (if (= valeur 2) 1 2))
	(increment pointeur)
	(append size (len_arr liste))
	}
times 1000 (proc_r update)
:= medium (mapi size item index (/ (* 1000 item) (+ 1 index)))
#print size
print medium
#print liste
#each liste i (print i)